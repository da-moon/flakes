# Ignore sync conflict artifacts generated by external sync tooling.
*.sync-conflict-*

# These flakes float on nixpkgs-unstable and are consumed with the caller's
# nixpkgs; we deliberately do not pin/commit a lock. The update-version.sh
# scripts pass --no-write-lock-file so builds never create one.
flake.lock
